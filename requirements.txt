# FastAPI and server
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6
jinja2==3.1.4

# Database
sqlalchemy==2.0.25
psycopg2-binary==2.9.9

# Validation
pydantic==2.5.3
pydantic-settings==2.1.0

# Auth — Supabase Auth (GoTrue) via HTTP; PyJWT solo para leer claims del token
PyJWT==2.10.1
httpx==0.26.0
python-dotenv==1.0.0

# Procesamiento de adjuntos. Reemplazan a jszip y unrar-js del frontend actual;
# postal-mime no tiene reemplazo porque los .eml los parsea el modulo `email`
# de la libreria estandar. zipfile tambien es estandar.
# rarfile necesita el binario `unrar` disponible en el sistema.
rarfile==4.2
python-docx==1.1.2
openpyxl==3.1.5

# Render del resumen ejecutivo, que viene en Markdown generado por la IA.
# bleach lo sanea antes de inyectarlo: el texto lo escribe un modelo a partir
# de documentos subidos por terceros, asi que se trata como no confiable.
markdown==3.7
bleach==6.2.0

# Hash de contrasenas: la aplicacion custodia sus propias credenciales
argon2-cffi==25.1.0

# Cifrado de secretos configurados desde la interfaz administrativa
cryptography==44.0.3
